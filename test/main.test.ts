// import { assert, assertEquals } from "https://deno.land/std@0.53.0/testing/asserts.ts";
// import { isCommandInPath } from "../src/main.ts";

const { test } = Deno;

// test("ls in $PATH", () => {
//   assertEquals(isCommandInPath('ls'), true)
// });

// test("zulu-yankee not in $PATH", () => {
//   assertEquals(isCommandInPath("zulu-yankee"), false);
// });
